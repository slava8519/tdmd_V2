# Expect: YamlParseError — `atoms.source` is required.

simulation:
  units: metal

atoms:
  path: ../al_fcc_small.data

potential:
  style: morse
  params:
    D: 0.2703
    alpha: 1.1646
    r0: 3.253
    cutoff: 8.0

integrator:
  style: velocity_verlet
  dt: 0.001

run:
  n_steps: 100
